import React from 'react';
import "./Counter.scss"
const Counter = () => {
    return (
        <div>
            счетчик!
            <button>fsdfdasfs</button>
        </div>
    );
};

export default Counter;